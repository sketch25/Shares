.wrapper
  = render "top_info"
  .Body
    .Body__items 
      %main
        .Posts-list
          %h1 投稿編集
          = render partial: 'form', locals: { post: @post }
          
      = render "side_bar"